# Airepro creative assets

Required filenames referenced by `templates/airepro-internship.html`:

| File | Role |
|------|------|
| `logo.svg` | Magenta Airepro wordmark + mark (top-left) |
| `hero.png` | Preferred student hero photo (right side; object-positioned) |
| `hero.svg` | Fallback illustration if `hero.png` is missing/unloadable |
| `sample-reference.png` | Original sample creative (reference only; not required at render time) |

Prefer a cropped student photo as `hero.png` (person on right, transparent or plain background). Keep filenames stable so the HTML template keeps working.
